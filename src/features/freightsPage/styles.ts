import { CSSProperties } from "react"

const container: CSSProperties = {
    padding: "100px 20px 50px 20px",
}
const subscribeContainer: CSSProperties = {
    display: "flex",
    width: "100%",
    margin: "40px 0px",
}

export default {
    container,
    subscribeContainer,
}